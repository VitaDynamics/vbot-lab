// Derived from the foxglove-sdk FlatBuffers schemas
// (https://github.com/foxglove/foxglove-sdk), licensed under the MIT License.
// Converted/adapted for Aorta. See NOTICE and docs/licensing.md.

// Generated by https://github.com/foxglove/foxglove-sdk

include "Time.fbs";
include "schemas/system/aorta_base.fbs";
include "schemas/shared/user_interaction/user_interaction_context.fbs";

namespace foxglove;

/// A single block of an audio bitstream
table RawAudio {
  /// Timestamp of the start of the audio block
  timestamp:Time (id: 0);

  /// Audio data. The samples in the data must be interleaved and little-endian
  data:[uint8] (id: 1);

  /// Audio format. Only 'pcm-s16' is currently supported
  format:string (id: 2);

  /// Sample rate in Hz
  sample_rate:uint32 (id: 3);

  /// Number of channels in the audio block
  number_of_channels:uint32 (id: 4);

  /// Aorta transport metadata header (Aorta extension field).
  aorta_header:aorta.sys.AortaHeader (id: 5);

  /// Optional event identity shared by all frames in one UWB voice turn.
  user_interaction_context:aorta.user_interaction.common.UserInteractionContext (id: 6);
}

root_type RawAudio;
