# 编辑器开发容器

<p align="center"><a href="README.md">English</a> | 中文</p>

使用本目录配置打开仓库时，复用 `docker/compose.yaml` 的 dev 服务。
启动开发环境时不会自动安装 SDK 或连接设备。

镜像前提、权限和网络边界见[容器说明](../docker/README.zh-CN.md)。
打开容器后，先检查工具并构建所选示例，再进行设备部署。
